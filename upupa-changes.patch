diff --git a/src/layouts/TourLayout.astro b/src/layouts/TourLayout.astro
index 68a5fd9..66b8b9f 100644
--- a/src/layouts/TourLayout.astro
+++ b/src/layouts/TourLayout.astro
@@ -6,6 +6,7 @@ export interface Stop {
   title: string;
   desc: string;
   image?: string;
+  images?: string[];
 }
 export interface Day {
   dayNumber: string; // "01", "02"
@@ -44,8 +45,6 @@ interface Props {
 const { label, title, summary, heroImage, duration, languages = [], pricing = null, days, included, tips } = Astro.props;
 
 const base = import.meta.env.BASE_URL;
-
-const LANG_NAMES: Record<string, string> = { en: "English", ru: "Russian", am: "Armenian" };
 ---
 
 <BaseLayout title={`${title} — Upupa Travel`} description={summary} current="tours">
@@ -97,11 +96,20 @@ const LANG_NAMES: Record<string, string> = { en: "English", ru: "Russian", am: "
                     <div class="absolute -left-[2.35rem] top-1 w-4 h-4 bg-secondary rounded-full"></div>
                     <h4 class="font-label-md text-label-md text-secondary uppercase mb-2">{stop.time} — {stop.title}</h4>
                     <p class="font-body-md text-body-md text-on-surface-variant mb-4">{stop.desc}</p>
-                    {stop.image && (
-                      <div class="aspect-video w-full overflow-hidden">
-                        <img src={stop.image} class="w-full h-full object-cover hover:scale-105 transition-transform duration-700" alt={stop.title} />
-                      </div>
-                    )}
+                    {(() => {
+                      const stopImages = stop.images?.length ? stop.images : stop.image ? [stop.image] : [];
+                      if (stopImages.length === 0) return null;
+                      const cols = stopImages.length === 1 ? "grid-cols-2 sm:grid-cols-3" : stopImages.length === 2 ? "grid-cols-2" : "grid-cols-2 sm:grid-cols-3";
+                      return (
+                        <div class={`grid ${cols} gap-2`}>
+                          {stopImages.map((src) => (
+                            <div class="aspect-square overflow-hidden rounded-lg">
+                              <img src={src} class="w-full h-full object-cover hover:scale-105 transition-transform duration-700" alt={stop.title} />
+                            </div>
+                          ))}
+                        </div>
+                      );
+                    })()}
                   </div>
                 ))}
               </div>
@@ -111,14 +119,18 @@ const LANG_NAMES: Record<string, string> = { en: "English", ru: "Russian", am: "
       </div>
 
       <!-- Sidebar -->
-      <div class="lg:col-span-4 space-y-stack-lg">
+      <div class="lg:col-span-4">
+        <div class="lg:sticky lg:top-28 space-y-stack-lg">
         <!-- Price Calculator -->
         <div class="bg-primary p-stack-lg shadow-xl" id="price-calc" data-pricing={JSON.stringify(pricing)}>
           <h3 class="font-headline-sm text-headline-sm text-white mb-stack-sm">Check Price</h3>
           <p class="text-white/70 font-caption text-caption mb-stack-md">Price adjusts automatically for your group size.</p>
 
           <div class="mb-4">
-            <label class="block font-label-md text-label-md text-white mb-2 uppercase">Number of People</label>
+            <label class="flex items-center gap-1.5 font-label-md text-label-md text-white mb-2 uppercase">
+              <span>Number of People</span>
+              <span class="material-symbols-outlined text-[16px] normal-case text-white/60 cursor-help" title="Total number of travelers in your group">info</span>
+            </label>
             <div class="flex items-center gap-3">
               <button type="button" id="pc-minus" class="w-10 h-10 bg-white/10 border border-white/20 text-white text-xl hover:bg-white/20 transition-colors" aria-label="Decrease">−</button>
               <input id="pc-people" type="number" min="1" max="19" value="2" class="w-16 text-center bg-white/10 border border-white/20 text-white px-2 py-2 [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none" />
@@ -126,25 +138,20 @@ const LANG_NAMES: Record<string, string> = { en: "English", ru: "Russian", am: "
             </div>
           </div>
 
-          <div class="mb-4">
+          <div class="mb-5 relative" id="pc-guide-wrap">
             <label class="block font-label-md text-label-md text-white mb-2 uppercase">Guide</label>
-            <select id="pc-guide" class="w-full bg-white/10 border border-white/20 text-white px-4 py-3 focus:ring-secondary focus:border-secondary">
-              <option value="guide" class="text-on-surface">With guide</option>
-              <option value="noGuide" class="text-on-surface">Without guide</option>
-            </select>
+            <button type="button" id="pc-guide-btn" class="w-full flex items-center justify-between gap-3 bg-white/10 border border-white/20 text-white px-4 py-3 hover:bg-white/20 transition-colors" aria-haspopup="listbox" aria-expanded="false">
+              <span id="pc-guide-label" class="flex items-center gap-2">No guide</span>
+              <span class="material-symbols-outlined text-[20px] transition-transform" id="pc-guide-chevron">expand_more</span>
+            </button>
+            <ul id="pc-guide-list" role="listbox" class="hidden absolute z-30 mt-1 w-full bg-surface-container-lowest border border-border-muted shadow-xl overflow-hidden">
+              <li data-value="noGuide" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer text-on-surface hover:bg-surface-container transition-colors">No guide</li>
+              <li data-value="guide:en" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer text-on-surface hover:bg-surface-container transition-colors border-t border-border-muted"><span>🇬🇧</span> Guide — English</li>
+              <li data-value="guide:ru" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer text-on-surface hover:bg-surface-container transition-colors border-t border-border-muted"><span>🇷🇺</span> Guide — Russian</li>
+              <li data-value="guide:am" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer text-on-surface hover:bg-surface-container transition-colors border-t border-border-muted"><span>🇦🇲</span> Guide — Armenian</li>
+            </ul>
           </div>
 
-          {languages.length > 0 && (
-            <div class="mb-5">
-              <p class="font-label-md text-label-md text-white mb-2 uppercase">Guide Language</p>
-              <div class="flex gap-2 flex-wrap">
-                {languages.map((l) => (
-                  <span class="bg-white/10 border border-white/20 text-white text-caption px-3 py-1 uppercase">{LANG_NAMES[l] ?? l}</span>
-                ))}
-              </div>
-            </div>
-          )}
-
           <div class="border-t border-white/20 pt-4">
             <p class="text-white/70 text-caption font-caption uppercase tracking-wider mb-1" id="pc-label">Price for 2 persons</p>
             <p class="text-white text-headline-sm font-headline-sm" id="pc-total">Contact for price</p>
@@ -181,6 +188,7 @@ const LANG_NAMES: Record<string, string> = { en: "English", ru: "Russian", am: "
             ))}
           </div>
         </div>
+        </div>
       </div>
     </div>
   </section>
@@ -203,16 +211,51 @@ const LANG_NAMES: Record<string, string> = { en: "English", ru: "Russian", am: "
   if (calc) {
     const pricing = JSON.parse(calc.dataset.pricing || "null");
     const peopleInput = document.getElementById("pc-people") as HTMLInputElement;
-    const guideSelect = document.getElementById("pc-guide") as HTMLSelectElement;
     const minusBtn = document.getElementById("pc-minus");
     const plusBtn = document.getElementById("pc-plus");
     const totalEl = document.getElementById("pc-total");
     const labelEl = document.getElementById("pc-label");
 
+    // Combined guide + language dropdown
+    const guideWrap = document.getElementById("pc-guide-wrap")!;
+    const guideBtn = document.getElementById("pc-guide-btn")!;
+    const guideLabel = document.getElementById("pc-guide-label")!;
+    const guideChevron = document.getElementById("pc-guide-chevron")!;
+    const guideList = document.getElementById("pc-guide-list")!;
+    const guideItems = Array.from(guideList.querySelectorAll("li"));
+    let guideValue = "noGuide"; // noGuide | guide:en | guide:ru | guide:am
+
+    function closeGuideList() {
+      guideList.classList.add("hidden");
+      guideChevron.classList.remove("rotate-180");
+      guideBtn.setAttribute("aria-expanded", "false");
+    }
+
+    guideBtn.addEventListener("click", () => {
+      const willOpen = guideList.classList.contains("hidden");
+      guideList.classList.toggle("hidden");
+      guideChevron.classList.toggle("rotate-180", willOpen);
+      guideBtn.setAttribute("aria-expanded", String(willOpen));
+    });
+
+    guideItems.forEach((li) => {
+      li.addEventListener("click", () => {
+        guideValue = li.getAttribute("data-value") || "noGuide";
+        guideLabel.innerHTML = li.innerHTML;
+        guideItems.forEach((o) => o.classList.toggle("bg-surface-container", o === li));
+        closeGuideList();
+        update();
+      });
+    });
+
+    document.addEventListener("click", (e) => {
+      if (!guideWrap.contains(e.target as Node)) closeGuideList();
+    });
+
     function update() {
       const people = Math.max(1, Math.min(19, parseInt(peopleInput.value, 10) || 1));
       peopleInput.value = String(people);
-      const withGuide = guideSelect.value === "guide";
+      const withGuide = guideValue.startsWith("guide:");
       const total = priceFor(pricing, people, withGuide);
       labelEl!.textContent = `Price for ${people} ${people === 1 ? "person" : "persons"}`;
       totalEl!.textContent = total == null ? "Contact for price" : formatAMD(total);
@@ -227,7 +270,6 @@ const LANG_NAMES: Record<string, string> = { en: "English", ru: "Russian", am: "
       update();
     });
     peopleInput.addEventListener("input", update);
-    guideSelect.addEventListener("change", update);
     update();
   }
 </script>
\ No newline at end of file
diff --git a/src/pages/index.astro b/src/pages/index.astro
index d3c405d..4865a0e 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -85,29 +85,29 @@ const base = import.meta.env.BASE_URL;
 <!-- Search / Filter Bar -->
 <div class="bg-surface-container-low border border-border-muted rounded-lg p-6 mb-12 flex flex-wrap items-end gap-6 justify-center" id="tours-search">
 <div>
-<label class="block text-label-md font-label-md text-primary uppercase mb-2">People</label>
-<div class="flex items-center gap-2">
-<button type="button" id="ts-minus" class="w-9 h-9 border border-border-muted text-primary hover:bg-surface-container transition-colors" aria-label="Decrease">−</button>
-<input id="ts-people" type="number" min="1" max="19" value="2" class="w-14 text-center border border-border-muted rounded p-2 [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none" />
-<button type="button" id="ts-plus" class="w-9 h-9 border border-border-muted text-primary hover:bg-surface-container transition-colors" aria-label="Increase">+</button>
+<label class="flex items-center gap-1.5 text-label-md font-label-md text-primary uppercase mb-2">
+<span>Number of people</span>
+<span class="material-symbols-outlined text-[16px] normal-case text-on-surface-variant cursor-help" title="Total number of travelers in your group">info</span>
+</label>
+<div class="flex items-center gap-2 border border-border-muted rounded-lg bg-surface-container-lowest px-2 py-1.5">
+<button type="button" id="ts-minus" class="w-8 h-8 flex items-center justify-center text-primary hover:bg-surface-container rounded transition-colors" aria-label="Decrease">−</button>
+<input id="ts-people" type="number" min="1" max="19" value="2" class="w-10 text-center bg-transparent [appearance:textfield] [&::-webkit-outer-spin-button]:appearance-none [&::-webkit-inner-spin-button]:appearance-none" />
+<button type="button" id="ts-plus" class="w-8 h-8 flex items-center justify-center text-primary hover:bg-surface-container rounded transition-colors" aria-label="Increase">+</button>
 </div>
 </div>
-<div>
+<div class="relative" id="ts-guide-wrap">
 <label class="block text-label-md font-label-md text-primary uppercase mb-2">Guide</label>
-<select id="ts-guide" class="border border-border-muted rounded p-2.5 min-w-[150px]">
-<option value="any">Any</option>
-<option value="guide">With guide</option>
-<option value="noGuide">Without guide</option>
-</select>
-</div>
-<div>
-<label class="block text-label-md font-label-md text-primary uppercase mb-2">Language</label>
-<select id="ts-lang" class="border border-border-muted rounded p-2.5 min-w-[150px]">
-<option value="any">Any</option>
-<option value="en">English</option>
-<option value="ru">Russian</option>
-<option value="am">Armenian</option>
-</select>
+<button type="button" id="ts-guide-btn" class="w-full min-w-[220px] flex items-center justify-between gap-4 border border-border-muted rounded-lg bg-surface-container-lowest px-4 py-2.5 hover:bg-surface-container transition-colors" aria-haspopup="listbox" aria-expanded="false">
+<span id="ts-guide-label" class="flex items-center gap-2 text-on-surface">Any guide option</span>
+<span class="material-symbols-outlined text-[20px] text-on-surface-variant transition-transform" id="ts-guide-chevron">expand_more</span>
+</button>
+<ul id="ts-guide-list" role="listbox" class="hidden absolute z-30 mt-1 w-full bg-surface-container-lowest border border-border-muted rounded-lg shadow-xl overflow-hidden">
+<li data-value="any" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer hover:bg-surface-container transition-colors">Any guide option</li>
+<li data-value="noGuide" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer hover:bg-surface-container transition-colors border-t border-border-muted">No guide</li>
+<li data-value="guide:en" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer hover:bg-surface-container transition-colors"><span>🇬🇧</span> English</li>
+<li data-value="guide:ru" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer hover:bg-surface-container transition-colors"><span>🇷🇺</span> Russian</li>
+<li data-value="guide:am" role="option" class="px-4 py-2.5 flex items-center gap-2 cursor-pointer hover:bg-surface-container transition-colors"><span>🇦🇲</span> Armenian</li>
+</ul>
 </div>
 </div>
 
@@ -131,15 +131,55 @@ const base = import.meta.env.BASE_URL;
 
   const grid = document.getElementById("tours-grid");
   const peopleInput = document.getElementById("ts-people");
-  const guideSelect = document.getElementById("ts-guide");
-  const langSelect = document.getElementById("ts-lang");
   const minusBtn = document.getElementById("ts-minus");
   const plusBtn = document.getElementById("ts-plus");
 
+  // Combined guide + language dropdown
+  const guideWrap = document.getElementById("ts-guide-wrap");
+  const guideBtn = document.getElementById("ts-guide-btn");
+  const guideLabel = document.getElementById("ts-guide-label");
+  const guideChevron = document.getElementById("ts-guide-chevron");
+  const guideList = document.getElementById("ts-guide-list");
+  const guideItems = Array.from(guideList.querySelectorAll("li"));
+  let guideValue = "any"; // any | noGuide | guide:en | guide:ru | guide:am
+
+  function closeGuideList() {
+    guideList.classList.add("hidden");
+    guideChevron.classList.remove("rotate-180");
+    guideBtn.setAttribute("aria-expanded", "false");
+  }
+
+  guideBtn.addEventListener("click", () => {
+    const willOpen = guideList.classList.contains("hidden");
+    guideList.classList.toggle("hidden");
+    guideChevron.classList.toggle("rotate-180", willOpen);
+    guideBtn.setAttribute("aria-expanded", String(willOpen));
+  });
+
+  guideItems.forEach((li) => {
+    li.addEventListener("click", () => {
+      guideValue = li.dataset.value;
+      guideLabel.innerHTML = li.innerHTML;
+      guideItems.forEach((o) => o.classList.toggle("bg-surface-container", o === li));
+      closeGuideList();
+      render();
+    });
+  });
+
+  document.addEventListener("click", (e) => {
+    if (!guideWrap.contains(e.target)) closeGuideList();
+  });
+
   function render() {
     const people = Math.max(1, Math.min(19, parseInt(peopleInput.value, 10) || 1));
-    const guideFilter = guideSelect.value;
-    const langFilter = langSelect.value;
+    let guideFilter = "any";
+    let langFilter = "any";
+    if (guideValue === "noGuide") {
+      guideFilter = "noGuide";
+    } else if (guideValue.startsWith("guide:")) {
+      guideFilter = "guide";
+      langFilter = guideValue.split(":")[1];
+    }
 
     const visible = TOURS.filter((t) => {
       if (langFilter !== "any" && !t.languages.includes(langFilter)) return false;
@@ -163,8 +203,9 @@ const base = import.meta.env.BASE_URL;
         ? `Price for ${people} ${personLabel}: ${formatAMD(total)}`
         : `Contact for price`;
 
-      const card = document.createElement("div");
-      card.className = "group bg-surface border border-border-muted rounded-lg overflow-hidden transition-all hover:shadow-lg flex flex-col";
+      const card = document.createElement("a");
+      card.href = `${base}tours/${t.slug}`;
+      card.className = "group bg-surface border border-border-muted rounded-lg overflow-hidden transition-all hover:shadow-lg hover:border-secondary/40 flex flex-col";
       card.innerHTML = `
         <div class="h-64 overflow-hidden relative">
           <div class="absolute top-4 left-4 z-10">
@@ -173,12 +214,12 @@ const base = import.meta.env.BASE_URL;
           <img alt="${t.title}" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" src="${t.image}">
         </div>
         <div class="p-6 flex flex-col flex-grow">
-          <h3 class="font-headline-sm text-primary mb-2">${t.title}</h3>
-          <p class="text-on-surface-variant text-body-md mb-4 flex-grow">${t.desc}</p>
+          <h3 class="font-headline-sm text-primary mb-2 group-hover:text-secondary transition-colors">${t.title}</h3>
+          <p class="text-on-surface-variant text-body-md mb-4 flex-grow group-hover:text-on-surface transition-colors">${t.desc}</p>
           <p class="text-primary font-label-md text-label-md mb-4">${priceLine}</p>
-          <a class="text-secondary font-bold flex items-center gap-1 group/btn hover:gap-3 transition-all" href="${base}tours/${t.slug}">
+          <span class="text-secondary font-bold flex items-center gap-1 group-hover:gap-3 transition-all">
             <span>See more</span> <span class="material-symbols-outlined text-[18px]">arrow_forward</span>
-          </a>
+          </span>
         </div>
       `;
       grid.appendChild(card);
@@ -194,8 +235,6 @@ const base = import.meta.env.BASE_URL;
     render();
   });
   peopleInput.addEventListener("input", render);
-  guideSelect.addEventListener("change", render);
-  langSelect.addEventListener("change", render);
 
   render();
 </script>
